# Frontend module

Vue 3 SPA with nested expense routes (`/expenses/add`, `/expenses/list`), shop/item typeahead, complementary per-row shares, Jalali calendar picker, monthly stats table, integer `formatMoney`, and footer GitHub link to ArminDashti.
