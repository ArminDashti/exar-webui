# Local deploy settings — YAML only (no CLI -- flags).

stack_name: exar
image_tag: exar-web:latest
compose_file: ../../docker-compose.yml
dockerfile: ../../Dockerfile
docker_network: exar-net
internal_port: "8080"
publish_port: "8084"
delete_volume: "no"
delete_image: "yes"
